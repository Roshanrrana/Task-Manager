{
  "name": "taskpilot",
  "private": true,
  "description": "TaskPilot — run backend + frontend together",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start:api": "npm start --prefix backend",
    "build:web": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
